import React from 'react';
import { action } from '@storybook/addon-actions';
import { TextBox } from '../components/TextBox';


export default {
  title: 'TextBox',
};

export const DefaultTextBox = () => <TextBox />;
